#include <iostream>
using namespace std;

int main() {
	//v = bh / 3
	double num;
	double num2;
	double input;
	cout << " what is the base of the cone?" << endl;
	cin >> num;
	cout << "what is the height of the cone?" << endl;
	cin >> num2;
	input = num*num2 / 3;
	cout << "the volume of the cone is" << input << endl;

}